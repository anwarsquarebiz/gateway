/**
 * Vivid gradient header for data tables — matches app shell (indigo / violet).
 * Apply to `<thead>`; header cells inherit `text-white`.
 */
export const tableTheadClassName =
    'border-b border-indigo-400/40 bg-gradient-to-r from-violet-600 to-indigo-600 text-white shadow-sm dark:border-indigo-500/30 dark:from-violet-500 dark:to-indigo-600';
